import * as React from 'react';
import TextField from '@mui/material/TextField';

export default function SearchField() {
    return (
        <TextField size="small" fullWidth id="outlined-basic" label="Search by Object name" variant="outlined" />
    );
}
